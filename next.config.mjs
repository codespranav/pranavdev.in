/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['res.cloudinary.com'], // allow Next.js Image component to load images from Cloudinary
  },
};

export default nextConfig;
